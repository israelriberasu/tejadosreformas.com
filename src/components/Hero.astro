<!-- Hero.astro — Hero section with CTAs -->

<section id="hero" class="hero">
  <div class="hero__overlay"></div>
  <div class="hero__content">
    <h1 class="hero__title">Expertos en Construcción y Reparación de Tejados en Burgos</h1>
    <p class="hero__subtitle">
      Reformas integrales, albañilería, fontanería y pequeñas reparaciones para hogares y
      empresas en toda la provincia. Presupuesto totalmente gratuito y sin compromiso.
    </p>
    <div class="hero__actions">
      <a href="tel:622299746" class="btn btn--primary">
        Llamar Ahora
      </a>
      <a
        href="https://wa.me/34622299746?text=Hola%2C%20solicito%20presupuesto%20para%20un%20trabajo%20en%20Burgos"
        class="btn btn--secondary"
        target="_blank"
        rel="noopener noreferrer"
      >
        Solicitar Presupuesto por WhatsApp
      </a>
    </div>
  </div>
</section>

<style>
  /* ---- Hero ---- */
  .hero {
    position: relative;
    min-height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: var(--color-primary, #1a3254);
    color: #ffffff;
    padding: 2rem;
    overflow: hidden;
  }

  /* Subtle dot pattern overlay */
  .hero__overlay {
    position: absolute;
    inset: 0;
    opacity: 0.06;
    background-image: radial-gradient(circle at 25% 50%, #ffffff 1px, transparent 1px);
    background-size: 48px 48px;
    pointer-events: none;
  }

  /* Decorative gradient glow */
  .hero::after {
    content: '';
    position: absolute;
    top: -40%;
    right: -20%;
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(228, 121, 35, 0.12) 0%, transparent 70%);
    pointer-events: none;
  }

  .hero__content {
    position: relative;
    z-index: 1;
    max-width: 820px;
    text-align: center;
  }

  .hero__title {
    font-size: clamp(1.75rem, 5vw, 3rem);
    line-height: 1.2;
    margin-bottom: 1.25rem;
    font-weight: 700;
    letter-spacing: -0.02em;
  }

  .hero__subtitle {
    font-size: clamp(1rem, 2.5vw, 1.2rem);
    line-height: 1.65;
    margin-bottom: 2.5rem;
    opacity: 0.92;
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    justify-content: center;
  }

  /* ---- Shared button styles ---- */
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 1rem 2rem;
    border-radius: var(--border-radius, 8px);
    font-size: 1.05rem;
    font-weight: 600;
    line-height: 1.2;
    text-decoration: none;
    cursor: pointer;
    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease,
      background-color 0.2s ease;
    border: none;
  }

  .btn:hover {
    transform: translateY(-3px);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.18);
  }

  .btn:active {
    transform: translateY(-1px);
  }

  .btn--primary {
    background-color: var(--color-accent, #e47923);
    color: #ffffff;
  }

  .btn--primary:hover {
    background-color: #d06a1a;
  }

  .btn--secondary {
    background-color: transparent;
    color: #ffffff;
    border: 2px solid #ffffff;
  }

  .btn--secondary:hover {
    background-color: rgba(255, 255, 255, 0.10);
    box-shadow: 0 6px 20px rgba(255, 255, 255, 0.08);
  }

  /* ---- Responsive ---- */
  @media (max-width: 600px) {
    .hero {
      min-height: 70vh;
      padding: 1.5rem;
    }

    .hero__title {
      font-size: clamp(1.5rem, 6vw, 1.9rem);
    }

    .hero__subtitle {
      font-size: 0.95rem;
      margin-bottom: 2rem;
    }

    .hero__actions {
      flex-direction: column;
      align-items: stretch;
    }

    .btn {
      padding: 0.9rem 1.25rem;
      font-size: 1rem;
    }
  }
</style>
