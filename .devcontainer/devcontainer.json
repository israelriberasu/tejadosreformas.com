{
  "name": "TejadosReformas",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "forwardPorts": [4321],
  "postCreateCommand": "if [ -f package.json ]; then npm install --no-audit --no-fund; else echo 'No package.json found. Ready to create Astro app!'; fi",
  "remoteUser": "node",
  "customizations": {
    "vscode": {
      "extensions": [
        "astro-build.astro-vscode"
      ]
    }
  }
}